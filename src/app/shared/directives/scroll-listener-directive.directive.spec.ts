import { ScrollListenerDirectiveDirective } from './scroll-listener-directive.directive';

describe('ScrollListenerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollListenerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
